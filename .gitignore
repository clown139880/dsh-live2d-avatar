node_modules/
lib/
*.log
.DS_Store
.npm-cache/

# Megumi and other third-party character assets are local-only until their
# redistribution rights are explicitly confirmed.
assets/models/*
!assets/models/.gitkeep
!assets/models/haru/
!assets/models/haru/**

# --- Local scratch / analysis artifacts (kept on disk, never published) ---
# Extracted Live2D model dumps, cloth render probes and CAD/paint scratch.
live2d-extracted/
cloth-parts/
parts/
.pnpm-store/

# Render/report scratch outputs and one-off analysis dumps.
render-*.png
pink-cloth_*.png
pink-cloth_*.txt
pink-006-report.txt

# C# scratch helpers (not part of the TypeScript package).
PasteBack.cs
RegionAudit.cs
tools/PasteBack.cs
tools/upload-asset-v2.mjs

# Generated model-browser artifacts (regenerated by gen-manifest.mjs from the
# local-only extracted Live2D library).
tools/model-browser/manifest.json
tools/model-browser/names.json

# Internal research docs that document pulling third-party game Live2D
# assets; these stay out of the public package (mirrors the assets/models/*
# local-only policy).
.scratch-docs/
